---
// WhatsAppFloat.astro
---

<!-- Botón WhatsApp flotante -->
<div class="whatsapp-float">
  <a 
    href="https://wa.me/573195895858?text=Hola,%20me%20gustaría%20más%20información%20sobre%20Pixel%20Living" 
    target="_blank"
    class="whatsapp-button"
    aria-label="Contactar por WhatsApp"
  >
    <svg 
      xmlns="http://www.w3.org/2000/svg" 
      viewBox="0 0 448 512" 
      class="whatsapp-icon"
    >
      <path 
        fill="currentColor" 
        d="M380.9 97.1C339 55.1 283.2 32 223.9 32c-122.4 0-222 99.6-222 222 0 39.1 10.2 77.3 29.6 111L0 480l117.7-30.9c32.4 17.7 68.9 27 106.1 27h.1c122.3 0 222-99.6 222-222 0-59.3-23.1-115-65-156.9zM223.9 413.3c-33.2 0-65.7-8.9-94-25.7l-6.7-4-69.8 18.3L72 335.1l-4.4-7.1c-18.5-29.4-28.2-63.3-28.2-98.2 0-101.7 82.8-184.5 184.6-184.5 49.3 0 95.6 19.2 130.4 54.1 34.8 34.9 56 81.2 56 130.5 0 101.8-84.9 184.4-186.5 184.4zm101.2-138.2c-5.5-2.8-32.8-16.2-37.9-18-5.1-1.9-8.8-2.8-12.5 2.8-3.7 5.6-14.3 18-17.6 21.8-3.2 3.7-6.5 4.2-12 1.4-32.6-16.3-54-29.1-75.5-66-5.7-9.8 5.7-9.1 16.3-30.3 1.8-3.7.9-6.9-.5-9.7-1.4-2.8-12.5-30.1-17.1-41.2-4.5-10.8-9.1-9.3-12.5-9.5-3.2-.2-6.9-.2-10.6-.2-3.7 0-9.7 1.4-14.8 6.9-5.1 5.6-19.4 19-19.4 46.3 0 27.3 19.9 53.7 22.6 57.4 2.8 3.7 39.1 59.7 94.8 83.8 35.2 15.2 49 16.5 66.6 13.9 10.7-1.6 32.8-13.4 37.4-26.4 4.6-13 4.6-24.1 3.2-26.4-1.3-2.5-5-3.9-10.5-6.6z"
      />
    </svg>
    
    <!-- Tooltip opcional -->
    <span class="whatsapp-tooltip">
      ¡Escríbenos!
    </span>
  </a>
</div>

<style>
  .whatsapp-float {
    position: fixed;
    bottom: 25px;
    right: 25px;
    z-index: 1000;
  }

  .whatsapp-button {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 60px;
    height: 60px;
    background: #FF9933;
    border-radius: 50%;
    color: white;
    text-decoration: none;
    box-shadow: 0 4px 20px rgba(255, 153, 51, 0.3);
    transition: all 0.3s ease;
    animation: bounce 2s infinite;
    z-index: 2;
  }

  .whatsapp-button:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 25px rgba(255, 153, 51, 0.5);
    background: #ff8800;
  }

  .whatsapp-icon {
    width: 32px;
    height: 32px;
    transition: transform 0.3s ease;
  }

  .whatsapp-button:hover .whatsapp-icon {
    transform: rotate(10deg) scale(1.1);
  }

  /* Tooltip */
  .whatsapp-tooltip {
    position: absolute;
    right: 70px;
    top: 50%;
    transform: translateY(-50%);
    background: #333;
    color: white;
    padding: 8px 12px;
    border-radius: 6px;
    font-size: 14px;
    font-weight: 500;
    white-space: nowrap;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    pointer-events: none;
  }

  .whatsapp-tooltip::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 100%;
    transform: translateY(-50%);
    border: 6px solid transparent;
    border-left-color: #333;
  }

  .whatsapp-button:hover .whatsapp-tooltip {
    opacity: 1;
    visibility: visible;
    transform: translateY(-50%) translateX(-5px);
  }

  /* Animaciones */
  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
    60% {
      transform: translateY(-5px);
    }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .whatsapp-float {
      bottom: 20px;
      right: 20px;
    }
    
    .whatsapp-button {
      width: 55px;
      height: 55px;
    }
    
    .whatsapp-icon {
      width: 28px;
      height: 28px;
    }
    
    .whatsapp-tooltip {
      display: none; /* Ocultar tooltip en móvil */
    }
  }

  /* Efecto de entrada */
  .whatsapp-float {
    animation: slideInUp 0.5s ease-out;
  }

  @keyframes slideInUp {
    from {
      transform: translateY(100px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  /* Efecto hover adicional */
  .whatsapp-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    border-radius: 50%;
    background: linear-gradient(45deg, #FF9933, #ffaa55);
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: -1;
  }

  .whatsapp-button:hover::before {
    opacity: 1;
  }

  /* Pulso de notificación */
  .whatsapp-button.notification {
    animation: pulse 1.5s infinite;
  }

  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 rgba(255, 153, 51, 0.7);
    }
    70% {
      box-shadow: 0 0 0 20px rgba(255, 153, 51, 0);
    }
    100% {
      box-shadow: 0 0 0 0 rgba(255, 153, 51, 0);
    }
  }
</style>

<script>
  // Agregar efecto de pulso ocasional
  const whatsappButton = document.querySelector('.whatsapp-button');
  
  if (whatsappButton) {
    // Pulso cada 10 segundos
    setInterval(() => {
      whatsappButton.classList.add('notification');
      setTimeout(() => {
        whatsappButton.classList.remove('notification');
      }, 3000);
    }, 10000);
    
    // Tracking de clicks (opcional)
    whatsappButton.addEventListener('click', () => {
      // Aquí puedes agregar analytics si quieres
      console.log('WhatsApp button clicked');
    });
  }
</script>