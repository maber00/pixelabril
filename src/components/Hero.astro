---
// Hero.astro OPTIMIZADO con messaging de alta conversión y responsive mobile
import heroImage1 from '../assets/pixel-frontal-002.webp';
import heroImage2 from '../assets/pixel-room-01.webp';
import heroImage3 from '../assets/pixel-recepcion.webp';
import heroImage4 from '../assets/pixel-terraza-hero-2.webp';
import heroImage5 from '../assets/pixel-room-02.jpg';
---

<section class="relative min-h-screen flex items-center overflow-hidden w-full" id="hero-section">
  <!-- Carousel de fondo -->
  <div class="absolute inset-0 z-0">
    <!-- Imágenes del carousel -->
    <div class="carousel-container">
      <div class="carousel-slide active">
        <div class="zoom-wrapper">
          <img
            src={heroImage1.src}
            alt="Pixel Living - Coliving moderno en Bogotá"
            class="zoom-image zoom-image-1"
          />
        </div>
      </div>
      <div class="carousel-slide">
        <div class="zoom-wrapper">
          <img
            src={heroImage2.src}
            alt="Apartaestudios amoblados 29m² en el norte de Bogotá"
            class="zoom-image zoom-image-2"
          />
        </div>
      </div>
      <div class="carousel-slide">
        <div class="zoom-wrapper">
          <img
            src={heroImage3.src}
            alt="Coliving sin codeudor en Zona Rosa Bogotá"
            class="zoom-image zoom-image-3"
          />
        </div>
      </div>
      <div class="carousel-slide">
        <div class="zoom-wrapper">
          <img
            src={heroImage4.src}
            alt="Terraza Pixel Living - Espacios compartidos"
            class="zoom-image zoom-image-4"
          />
        </div>
      </div>
      <div class="carousel-slide">
        <div class="zoom-wrapper">
          <img
            src={heroImage5.src}
            alt="Habitaciones completamente equipadas Pixel Living"
            class="zoom-image zoom-image-5"
          />
        </div>
      </div>
    </div>
    
    <!-- Overlay gradiente -->
    <div class="absolute inset-0 bg-gradient-to-r from-black/70 to-black/40 z-10"></div>
  </div>
  
  <!-- Contenido principal OPTIMIZADO -->
  <div class="container relative z-20 mt-32 mb-32 md:py-0">
    <div class="flex flex-col items-start text-left max-w-4xl">
      
      <!-- Título principal optimizado -->
      <h1 class="text-sm md:text-3xl font-normal leading-tight mb-1 text-white cal-display hero-title">
  <span class="text-pixel-orange text-xl md:text-4xl">Coliving Bogotá</span> 
  <span class="text-xs md:text-xl opacity-60">|</span> 
  <span class="text-pixel-yellow text-xl md:text-4xl">Pixel Living</span>
</h1>

      <!-- Subtítulo -->
      <h2 class="text-5xl md:text-6xl text-white mb-6 font-normal">
        <span class="block">Vive, crea y conecta</span>
        <span class="block text-2xl md:text-5xl text-pixel-yellow mt-1">en el norte de Bogotá.</span>
      </h2>
      
      <!-- Descripción principal -->
      <p class="text-lg md:text-xl text-gray-200 mb-6 max-w-2xl leading-relaxed">
        Pixel Living es tu coliving en Bogotá, donde encuentras apartaestudios modernos y amoblados, 
        todos los servicios incluidos y una comunidad vibrante en la ubicación más estratégica de la ciudad.
      </p>

      <!-- Nuevos beneficios destacados -->
      <div class="flex flex-wrap gap-2 mb-6">
        <span class="bg-pixel-yellow text-gray-900 px-3 py-1 rounded-full text-sm font-medium">✓ Sin codeudor</span>
        <span class="bg-pixel-orange text-orange-950 px-3 py-1 rounded-full text-sm font-medium">✓ Desde 1 mes</span>
        <span class="bg-pixel-blue text-blue-950 px-3 py-1 rounded-full text-sm font-medium">✓ Todo incluido</span>
        <span class="bg-pixel-green text-green-950 px-3 py-1 rounded-full text-sm font-medium">✓ Listo para habitar</span>
      </div>
      
      <!-- Descripción específica - OCULTA EN MÓVIL -->
      <p class="hidden md:block text-lg md:text-xl text-gray-200 mb-6 max-w-2xl">
        Disfruta la flexibilidad de arrendar desde un mes, sin codeudor y sin complicaciones.
      </p>

      <!-- Texto principal - OCULTO EN MÓVIL -->
      <p class="hidden md:block text-xl md:text-2xl text-white mb-4 font-semibold">
        Todo lo que necesitas para vivir, trabajar y crecer, está aquí.
      </p>

      <!-- Pregunta CTA - OCULTA EN MÓVIL -->
      <p class="hidden md:block text-lg text-pixel-yellow mb-8 font-medium">
        ¿Listo para vivir la experiencia Pixel Living?
      </p>
      
      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row gap-4">
        <!-- Botón principal - OCULTO EN MÓVIL, VISIBLE EN DESKTOP -->
        <a href="#reservas" class="hidden md:flex btn bg-pixel-yellow hover:bg-yellow-500 text-gray-900 text-lg font-semibold px-8 py-4 rounded-lg items-center justify-center">
          Reserva tu espacio ahora
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 ml-2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5 21 12m0 0-7.5 7.5M21 12H3" />
          </svg>
        </a>
        
        <!-- Botón secundario - SIEMPRE VISIBLE -->
        <a href="#espacios" class="btn bg-transparent border-2 border-white text-white hover:bg-white hover:text-gray-900 px-6 py-4 rounded-lg">
          Ver precios y disponibilidad
        </a>
      </div>

      <!-- Información de confianza -->
      <p class="text-sm text-gray-300 mt-4">
        Cupos limitados • Solo tu documento de identificación
      </p>
      <p class="text-xs text-gray-400 mt-2">
        Proceso de reserva en menos de 24 horas
      </p>
    </div>
  </div>
  
  <!-- Indicadores del carousel -->
  <div class="absolute bottom-20 invisible md:visible mb-5 left-1/2 transform -translate-x-1/2 z-30">
    <div class="flex space-x-3 carousel-indicators">
      <button class="carousel-dot active" data-slide="0" aria-label="Imagen 1"></button>
      <button class="carousel-dot" data-slide="1" aria-label="Imagen 2"></button>
      <button class="carousel-dot" data-slide="2" aria-label="Imagen 3"></button>
      <button class="carousel-dot" data-slide="3" aria-label="Imagen 4"></button>
      <button class="carousel-dot" data-slide="4" aria-label="Imagen 5"></button>
    </div>
  </div>
  
  <!-- Indicador de scroll -->
  <div class="absolute md:bottom-8 invisible md:visible md:left-1/2 transform -translate-x-1/2 text-white z-30">
    <a href="#vive-pixel" class="flex flex-col mt-30 items-center group">
      <span class="mb-2 text-sm font-medium group-hover:text-pixel-yellow transition-colors">Descubre por qué somos diferentes</span>
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6 animate-bounce">
        <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
      </svg>
    </a>
  </div>
</section>

<!-- Estilos mantenidos del original -->
<style>
  /* ===== TODOS LOS ESTILOS ORIGINALES DEL CAROUSEL ===== */
  .carousel-container {
    position: relative;
    width: 150%;
    height: 150vh;
    overflow: hidden;
  }
  
  /* Tamaño optimizado para laptop/desktop */
  @media (min-width: 1024px) {
    .carousel-container {
      width: 125%;
      height: 125vh;
    }
  }
  
  .carousel-slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    transition: opacity 1.2s ease-in-out;
    z-index: 0;
  }
  
  .carousel-slide.active {
    opacity: 1;
    z-index: 1;
  }
  
  .zoom-wrapper {
    width: 100%;
    height: 100%;
    overflow: hidden;
    position: relative;
  }
  
  .zoom-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: absolute;
    top: 0;
    left: 0;
  }
  
  .zoom-image-1 { animation: zoomEffect1 8s ease-in-out infinite alternate; }
  .zoom-image-2 { animation: zoomEffect2 10s ease-in-out infinite alternate; }
  .zoom-image-3 { animation: zoomEffect3 7s ease-in-out infinite alternate; }
  .zoom-image-4 { animation: zoomEffect4 9s ease-in-out infinite alternate; }
  .zoom-image-5 { animation: zoomEffect5 6s ease-in-out infinite alternate; }
  
  @keyframes zoomEffect1 {
    0% { transform: scale(1) translate(0, 0); }
    100% { transform: scale(1.1) translate(1%, 1%); }
  }
  
  @keyframes zoomEffect2 {
    0% { transform: scale(1) translate(1%, 0); }
    100% { transform: scale(1.1) translate(-1%, 1%); }
  }
  
  @keyframes zoomEffect3 {
    0% { transform: scale(1) translate(-1%, 1%); }
    100% { transform: scale(1.1) translate(1%, -1%); }
  }
  
  @keyframes zoomEffect4 {
    0% { transform: scale(1) translate(0, -1%); }
    100% { transform: scale(1.1) translate(-1%, 0); }
  }
  
  @keyframes zoomEffect5 {
    0% { transform: scale(1) translate(1%, 1%); }
    100% { transform: scale(1.1) translate(0, -1%); }
  }
  
  .carousel-indicators {
    backdrop-filter: blur(10px);
    background: rgba(0, 0, 0, 0.2);
    padding: 12px 20px;
    border-radius: 50px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    pointer-events: none;
    z-index: 50;
    position: relative;
  }
  
  .carousel-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    border: 2px solid rgba(255, 255, 255, 0.4);
    background: transparent;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    pointer-events: auto;
    z-index: 51;
  }
  
  .carousel-dot::before {
    content: '';
    position: absolute;
    top: -8px;
    left: -8px;
    right: -8px;
    bottom: -8px;
  }
  
  .carousel-dot:hover {
    border-color: rgba(255, 255, 255, 0.8);
    transform: scale(1.1);
  }
  
  .carousel-dot.active {
    background: #fbbf24;
    border-color: #fbbf24;
    box-shadow: 0 0 10px rgba(251, 191, 36, 0.5);
  }
  
  /* Responsive optimizado */
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
      line-height: 1.1;
    }
    
    .carousel-indicators {
      bottom: 16px;
      padding: 8px 16px;
    }
    
    .carousel-dot {
      width: 10px;
      height: 10px;
    }
    
    /* Grid responsive para beneficios */
    .grid-cols-2 {
      grid-template-columns: 1fr;
    }
  }
  
  /* Mejoras de accesibilidad */
  @media (prefers-reduced-motion: reduce) {
    .carousel-slide { transition: opacity 0.5s ease-in-out; }
    .zoom-image { animation: none !important; }
    .animate-pulse { animation: none !important; }
    .animate-bounce { animation: none !important; }
  }
  
  /* Focus states mejorados */
  .btn:focus {
    outline: 3px solid #ffd700;
    outline-offset: 2px;
  }
</style>

<!-- Script del carousel mantenido del original -->
<script is:inline>
// [MANTENER TODO EL SCRIPT ORIGINAL DEL CAROUSEL]
(function() {
  'use strict';
  
  function initHeroCarousel() {
    console.log('🎠 Inicializando Hero Carousel...');
    
    // ===== ELEMENTOS =====
    var slides = document.querySelectorAll('.carousel-slide');
    var dots = document.querySelectorAll('.carousel-dot');
    var heroSection = document.getElementById('hero-section');
    
    if (!slides.length || !dots.length) {
      console.error('❌ No se encontraron slides o dots');
      return;
    }
    
    // ===== CONFIGURACIÓN =====
    var CONFIG = {
      duration: 4000,
      fadeTime: 1200,
      currentSlide: 0,
      totalSlides: slides.length,
      isPlaying: true,
      intervalId: null,
      isTransitioning: false
    };
    
    console.log('📊 ' + CONFIG.totalSlides + ' slides encontrados');
    
    // ===== FUNCIONES =====
    function goToSlide(slideIndex) {
      if (slideIndex < 0 || slideIndex >= CONFIG.totalSlides) return;
      if (CONFIG.isTransitioning) return;
      if (slideIndex === CONFIG.currentSlide) return;
      
      CONFIG.isTransitioning = true;
      
      slides[CONFIG.currentSlide].classList.remove('active');
      dots[CONFIG.currentSlide].classList.remove('active');
      CONFIG.currentSlide = slideIndex;
      slides[CONFIG.currentSlide].classList.add('active');
      dots[CONFIG.currentSlide].classList.add('active');
      
      setTimeout(function() {
        CONFIG.isTransitioning = false;
      }, CONFIG.fadeTime);
      
      if (CONFIG.isPlaying) resetAutoplay();
    }
    
    function nextSlide() {
      var nextIndex = (CONFIG.currentSlide + 1) % CONFIG.totalSlides;
      goToSlide(nextIndex);
    }
    
    function startAutoplay() {
      stopAutoplay();
      CONFIG.intervalId = setInterval(function() {
        if (CONFIG.isPlaying && !CONFIG.isTransitioning) {
          nextSlide();
        }
      }, CONFIG.duration);
    }
    
    function stopAutoplay() {
      if (CONFIG.intervalId) {
        clearInterval(CONFIG.intervalId);
        CONFIG.intervalId = null;
      }
    }
    
    function resetAutoplay() {
      stopAutoplay();
      startAutoplay();
    }
    
    // ===== EVENT LISTENERS =====
    for (var i = 0; i < dots.length; i++) {
      var dot = dots[i];
      dot.setAttribute('data-slide-index', i.toString());
      
      dot.addEventListener('click', function(e) {
        e.preventDefault();
        var clickedIndex = parseInt(this.getAttribute('data-slide-index'));
        goToSlide(clickedIndex);
      });
      
      dot.setAttribute('tabindex', '0');
      dot.setAttribute('role', 'button');
      dot.setAttribute('aria-label', 'Ir a imagen ' + (i + 1));
      
      dot.addEventListener('keydown', function(e) {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault();
          var clickedIndex = parseInt(this.getAttribute('data-slide-index'));
          goToSlide(clickedIndex);
        }
      });
    }
    
    if (heroSection) {
      heroSection.addEventListener('mouseenter', function() {
        CONFIG.isPlaying = false;
      });
      
      heroSection.addEventListener('mouseleave', function() {
        CONFIG.isPlaying = true;
      });
    }
    
    document.addEventListener('keydown', function(e) {
      if (!heroSection) return;
      var rect = heroSection.getBoundingClientRect();
      var heroVisible = rect.top < window.innerHeight / 2;
      if (!heroVisible) return;
      
      if (e.key === 'ArrowLeft') {
        e.preventDefault();
        var prevIndex = CONFIG.currentSlide === 0 ? CONFIG.totalSlides - 1 : CONFIG.currentSlide - 1;
        goToSlide(prevIndex);
      } else if (e.key === 'ArrowRight') {
        e.preventDefault();
        nextSlide();
      }
    });
    
    document.addEventListener('visibilitychange', function() {
      if (document.hidden) {
        CONFIG.isPlaying = false;
        stopAutoplay();
      } else {
        CONFIG.isPlaying = true;
        startAutoplay();
      }
    });
    
    var observer = new IntersectionObserver(function(entries) {
      for (var j = 0; j < entries.length; j++) {
        var entry = entries[j];
        if (entry.isIntersecting) {
          CONFIG.isPlaying = true;
          startAutoplay();
        } else {
          CONFIG.isPlaying = false;
          stopAutoplay();
        }
      }
    }, { threshold: 0.5 });
    
    if (heroSection) observer.observe(heroSection);
    
    // ===== INICIALIZACIÓN =====
    slides[0].classList.add('active');
    dots[0].classList.add('active');
    startAutoplay();
    
    console.log('✅ Hero Carousel inicializado');
    
    // API para debug
    window.heroCarousel = {
      goToSlide: goToSlide,
      nextSlide: nextSlide,
      prevSlide: prevSlide,
      play: function() { CONFIG.isPlaying = true; startAutoplay(); },
      pause: function() { CONFIG.isPlaying = false; stopAutoplay(); },
      config: function() { return CONFIG; }
    };
  }
  
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initHeroCarousel);
  } else {
    initHeroCarousel();
  }
})();
</script>